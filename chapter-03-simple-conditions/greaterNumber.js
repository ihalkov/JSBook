function greaterNumber([num1, num2]) {
    num1 = parseInt(num1);
    num2 = parseInt(num2);

    if (num1 > num2) {
        console.log(num1);
    } else {
        console.log(num2);
    }
}